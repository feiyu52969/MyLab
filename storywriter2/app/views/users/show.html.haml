.col-xs-8#users-content
  = render partial: "users/header"
  .list-group
    - if @user.tweets.empty?
      .list-group-item
        まだストーリーはありません。
    = render partial: "users/story", collection: @user.tweets
